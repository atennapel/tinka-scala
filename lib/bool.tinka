let if-ind_ P b t f = elimBool P t f b;
let if_ {A} b t f = if b : \_. A then t else f;

let (!) (b : Bool) : Bool = if b then False else True;
let (&&) (a b : Bool) : Bool = if a then b else False;
let (||) (a b : Bool) : Bool = if a then True else b;

export
