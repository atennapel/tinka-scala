open #lib/enum (case);
open #lib/void (elimVoid);

mod {
  Bool2 = '[True, False];
  True2 : Bool2 = 'True;
  False2 : Bool2 = 'False;

  elimBool2 <k> (P : Bool2 -> Type k) (t : P True2) (f : P False2) (b : Bool2) : P b
    = case b P t (\x. case x (\y. P (TS y)) f (elimVoid (\y. P (TS y))));
}
